# Chess Analysis API Environment Variables

# OpenAI API Key for move explanations
OPENAI_API_KEY=your_openai_api_key_here

# Port for the server (automatically set by Railway/Render)
PORT=8000

# Optional: Stockfish path override (leave empty for auto-detection)
STOCKFISH_PATH=